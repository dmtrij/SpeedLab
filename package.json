{
  "name": "speedlab",
  "version": "1.0.0",
  "description": "Local and PSI-backed Lighthouse dashboard for sequential performance testing.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test:single": "node server.js --single",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "chrome-launcher": "^1.2.1",
    "express": "^4.21.2",
    "lighthouse": "^13.3.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
